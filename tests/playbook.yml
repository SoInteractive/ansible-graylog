---
- hosts: all
  tasks:
    - name: install testing suite
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - iproute
        - net-tools
        - mongodb

    - name: ensure mongodb has started
      service:
        name: mongodb
        enabled: yes
        state: started

- hosts: all
  roles:
    - ansible-graylog
  vars:
    graylog_password_salt: "testsalt_mustbemin16chars"
    graylog_address: "127.0.0.1"
    graylog_root:
      username: admin
      password: 'password'
      email: root@localhost

#    graylog_root.password_sha2: '5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8'
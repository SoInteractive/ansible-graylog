---
- hosts: all
  tasks:
    - name: install testing suite
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - iproute
        - net-tools

- hosts: all
  roles:
    - graylog
  vars:
    graylog_password_salt: "testsalt"
    graylog_address: "127.0.0.1"
